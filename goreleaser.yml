builds:
  - binary: terraform-provider-form3
    goos:
      - darwin
      - linux
      - windows
    goarch:
      - amd64
    hooks:
      post: ./scripts/goreleaser-post.sh
archive:
  format: zip
